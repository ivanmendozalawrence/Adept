# Copy to .env and fill in
GOOGLE_CLIENT_ID= 331912293459-tcbrtb1pu21dmfqvugetbkuca7if4a0n.apps.googleusercontent.com
GOOGLE_CLIENT_SECRET= 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
SESSION_SECRET=dev_change_me
PORT=4000
FRONTEND_ORIGIN=http://localhost:3000
SQLITE_PATH=../backend/db.sqlite3

# --- Email / SMTP configuration ---
# Required for sending invitation emails. Fill these in with your provider values.
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=
MAIL_FROM="Adept Invites <no-reply@example.com>"

# Optional: override the base URL used in invitation links (defaults to FRONTEND_ORIGIN)
# INVITE_BASE_URL=http://localhost:3000

# --- Gmail quick configuration (alternative to generic SMTP above) ---
# Use ONE method below (app password recommended). Remove SMTP_* vars if using Gmail.
# 1) App password (enable 2FA, then create an App Password in Google Account > Security):
GMAIL_USER=
GMAIL_APP_PASSWORD=
# 2) Basic password (legacy / may be blocked):
# GMAIL_PASS=
# 3) OAuth2 (advanced):
# GMAIL_OAUTH_CLIENT_ID=
# GMAIL_OAUTH_CLIENT_SECRET=
# GMAIL_OAUTH_REFRESH_TOKEN=
# GMAIL_OAUTH_ACCESS_TOKEN=
